mutation{
  executeCommand(device: "mid_csp/elt/master", command: "On", argin: ["1"]) {
    ok
    message
    output
	}
}

mutation{
  executeCommand(device: "mid_csp/elt/master", command: "On", argin: ["mid_csp_cbf/sub_elt/master"]) {
    ok
    message
    output
	}
}

runCanvas executeCommand
